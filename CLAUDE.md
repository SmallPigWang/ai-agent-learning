# AI Agent 学习项目规则

## 练习文件规范

**必须**使用 LeetCode 核心代码模式，禁止使用 `input()` 交互式输入：

- 需求写在文件顶部注释中
- 核心函数骨架已定义（签名 + docstring），函数体留空，不写 `pass` 或 `# TODO`
- `if __name__ == "__main__":` 块中包含测试用例，格式：
  `print(f"PASS/FAIL calculate('{输入}') -> {实际输出} | expected: {预期输出}")`
- 保存后 `python xxx.py` 一次性验证所有用例
- 函数体中只用 `return`，禁止 `print`（测试块负责输出）
- 文件名使用描述性英文命名（如 `calculator.py`、`list_stats.py`），禁止 `practice1.py` 等无意义编号
- **避免格式陷阱** — 练习聚焦当次学习目标，测试用例避免引入无关的格式坑（如特殊字符、编码、repr 引号细节等）；如需练习格式处理，单独作为专题练习

## 教学规则

每次出练习题目后，必须做三件事：

1. **打开文件** — 直接将练习文件在 IDE 中打开，让学员看到函数骨架
2. **只给骨架** — 文件中只有需求注释 + 函数签名 + docstring，函数体留空，**禁止写出完整代码**；测试用例可以预先写好
3. **输出知识点清单** — 用一个表格列出本题涉及的所有知识点（类别、知识点、一句话说明），附实现顺序提示
4. **新内容前先回顾** — 每次开始新的学习内容前，先总结之前**所有天数**已学内容的核心知识点，帮助学员衔接上下文；重点展示与今天学习内容相关的知识；直接输出即可，不写入文件

## 日志文件规范

- 每天的学习记录存放在 `logs/day-XX.md`（XX 为两位数，如 `day-01.md`）
- `每日学习日志.md` 只保留日志导航（文件路径 + 标题）和空白模板，不存放具体内容
- 每天学习结束后，新建 `logs/day-XX.md` 并在 `每日学习日志.md` 导航区追加一条链接
- **日志中记录时间范围** — 标题旁标注实际学习时间段（如 `08:00-09:15`），方便回顾效率
- **日志与看板联动** — 每次更新 `LEARNING_TRACKER.md` 后，必须同步更新当日 `logs/day-XX.md`（新增模块内容、练习文件、踩坑记录）
- **练习文件映射** — 每次出 `🔧 练习` 时，将对应的 `.py` 文件名写进 tracker，格式：`- [x] 🔧 练习：xxx 📎 file.py`；模块通关后补充所有文件链接
