# AI Agent 学习项目规则

## 练习文件规范

**必须**使用 LeetCode 核心代码模式，禁止使用 `input()` 交互式输入：

- 需求写在文件顶部注释中
- 核心函数骨架已定义（签名 + docstring），函数体留空，不写 `pass` 或 `# TODO`
- `if __name__ == "__main__":` 块中包含测试用例，格式：
  `print(f"PASS/FAIL calculate('{输入}') -> {实际输出} | expected: {预期输出}")`
- 保存后 `python xxx.py` 一次性验证所有用例
- 函数体中只用 `return`，禁止 `print`（测试块负责输出）
- 文件名使用描述性英文命名（如 `calculator.py`、`list_stats.py`），禁止 `practice1.py` 等无意义编号
- **头部标注知识点** — 练习文件顶部注释块末尾加一行 `# 知识点: ...`（竖线分隔的知识点清单），让文件自包含、GitHub 上可读
- **避免格式陷阱** — 练习聚焦当次学习目标，测试用例避免引入无关的格式坑（如特殊字符、编码、repr 引号细节等）；如需练习格式处理，单独作为专题练习

## 教学规则

每次出练习题目后，必须做三件事：

1. **打开文件** — 直接将练习文件在 IDE 中打开，让学员看到函数骨架
2. **只给骨架** — 文件中只有需求注释 + 函数签名 + docstring，函数体留空，**禁止写出完整代码**；测试用例可以预先写好
3. **输出知识点清单** — 用一个表格列出本题涉及的所有知识点（类别、知识点、一句话说明），附实现顺序提示
4. **新内容前先回顾** — 每次开始新的学习内容前，先总结之前**所有天数**已学内容的核心知识点，帮助学员衔接上下文；重点展示与今天学习内容相关的知识；直接输出即可，不写入文件
5. **阶段通关三件套** — 模块/阶段验收必须: 练习全 PASS + 🧩 独立挑战题（不教的新题）+ 📝 300 字费曼总结（写入当日日志）
6. **复习日** — 每 5 个学习日安排 1 个复习日: 跑 🔴 核心脚本并解释原理 + 扫 `PITFALLS.md` Top 5 + 重测 `STUDYNOTE_EVAL`（阶段 2 后）
7. **每模块配阅读** — 完成看板中的 📖 阅读条目（一手资料），读后 3 行总结写入当日日志

## 北极星项目

- 主线项目: 📚 **StudyNote 学习笔记 Agent**（能读懂本仓库 logs + KNOWLEDGE_BASE 并回答学习问题）
- 每个阶段练习尽量与主线项目挂钩；阶段完成标志中的 🎯 北极星条目 = 验收目标
- 详细定义见 `LEARNING_TRACKER.md` 顶部

## 知识库规范

- 新知识点同步追加到 `KNOWLEDGE_BASE.md`（按模块表格，更新"待补充模块"清单）
- 新踩坑同步追加到 `PITFALLS.md`（先登记到文末"新坑登记区"，再归入对应分类；高频坑更新 Top 5）

## 知识图谱规范

- 学习内容更新后（`KNOWLEDGE_BASE.md` / `PITFALLS.md` / `LEARNING_TRACKER.md` / `logs/` 任一变更），**必须**重新生成知识图谱数据，再刷新页面查看：
  1. 运行 `python scripts/knowledge-graph/gen_knowledge_graph.py`（重新解析全部 markdown → 更新 `knowledge-graph/data.js`）
  2. 浏览器打开/刷新 `knowledge-graph/index.html`（Ctrl+F5 强刷）
- **过期检查** — 每次会话开始时（或学员说"学完了/今天学了 X/更新图谱/帮我记录"等任意学习相关表述时），先对比 `knowledge-graph/data.js` 的生成时间与上述 4 个数据源文件的修改时间：任一数据源比 `data.js` 新，即视为图谱过期，**先重新生成再继续其他任务**
- 图谱数据自动维护：生成器只读 markdown、只写 `data.js`，前端代码（`index.html` / `app.js`）不需要改动
- 知识点"已学/未学"状态由日志与 tracker 勾选自动判定（知识点名称/关键词出现在 `logs/` 或该模块勾选行中 = 已学）
- "待补充"条目升格为正式模块后，必须从"待补充模块"清单中删除对应条目，否则图谱中会重复出现

## 日志文件规范

- 每天的学习记录存放在 `logs/day-XX.md`（XX 为两位数，如 `day-01.md`）
- `每日学习日志.md` 只保留日志导航（文件路径 + 标题）和空白模板，不存放具体内容
- 每天学习结束后，新建 `logs/day-XX.md` 并在 `每日学习日志.md` 导航区追加一条链接
- **日志中记录时间范围** — 标题旁标注实际学习时间段（如 `08:00-09:15`），方便回顾效率
- **日志与看板联动** — 每次更新 `LEARNING_TRACKER.md` 后，必须同步更新当日 `logs/day-XX.md`（新增模块内容、练习文件、踩坑记录）
- **练习文件映射** — 每次出 `🔧 练习` 时，将对应的 `.py` 文件名写进 tracker，格式：`- [x] 🔧 练习：xxx 📎 file.py`；模块通关后补充所有文件链接
- **导航区核心文件清单** — `每日学习日志.md` 导航区每天只列出 🔴 核心基建文件（构建 Agent 的基石，理解后可复用），带可点击链接，格式：

  ```
  - **Day N** — YYYY-MM-DD 核心内容 | [📝](logs/day-NN.md)
    - 🔴 [core_file.py](scripts/phaseX-xxx/core_file.py) — 一句话说明实现了什么功能
  ```
